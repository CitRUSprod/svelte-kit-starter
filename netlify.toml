[build]
    command = "npx pnpm i --store-dir=.pnpm && npx pnpm build:auto"
    publish = "build"

[build.environment]
    NODE_VERSION = "18.4.0"
    NPM_FLAGS = "-v"
    VITE_TITLE = "SvelteKit Starter"

[functions]
    node_bundler = "esbuild"
